# Groq (chat completions) - get a free key at https://console.groq.com
GROQ_API_KEY=
GROQ_MODEL=openai/gpt-oss-120b

# Local embedding model (fastembed) - no API key needed, downloaded on first use
EMBEDDING_MODEL=BAAI/bge-small-en-v1.5

# SQLite storage
MEMORY_DB_PATH=./companion.db
